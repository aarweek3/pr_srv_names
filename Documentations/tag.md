| ТЕХНИЧЕСКАЯ СПЕЦИФИКАЦИЯСистема CategoryTag & TagAggregator v3.5  •  Версия 4.0 (Final / Developer-Ready) |
| ---------------------------------------------------------------------------------------------------------------------------------------------- |

| Статус          | Final — готов к передаче в разработку                             |
| --------------------- | ------------------------------------------------------------------------------------------- |
| Версия          | 4.0                                                                                         |
| Дата              | Апрель 2026                                                                           |
| Основано на | v3.1 + критический аудит (противоречия, индексы, PK, UX) |

Давай объясню на конкретном примере — представь что ты делаешь каталог программ, как AppStore.

---

## Большая картина: зачем вообще эта система

Тебе нужно показывать теги на карточках программ — "Windows", "Бесплатно", "Графика" и т.д. Но теги нужно группировать, переводить на разные языки, и чтобы у каждого тега была своя страница в поиске Google.

---

## Уровень 1 — Категории и Теги (полки и этикетки)

Представь книжный магазин. Есть **полки** — "Операционные системы", "Лицензия", "Тип приложения". На каждой полке лежат **книги с этикетками** — "Windows", "macOS", "Бесплатно", "Платно".

В коде это:

* **CategoryTag** = полка. У неё есть цвет (синяя полка) и иконка.
* **Tag** = этикетка на книге. Привязана ровно к одной полке.

Тег **обязан** принадлежать категории — нельзя создать тег "в воздухе".

---

## Уровень 2 — Наследование цвета и иконки

Допустим, категория "Лицензия" — зелёная с иконкой 💰.

Ты создаёшь тег "Бесплатно" и не задаёшь ему цвет — он автоматически становится зелёным с 💰, как у родителя.

Но тег "Enterprise" ты хочешь сделать золотым — просто задаёшь ему свой цвет, и наследование отключается именно для него.

Это происходит  **не в базе данных** , а в момент отдачи данных клиенту — в маппере. В базе лежит буквально строка `"inherit"`, и маппер при чтении подставляет цвет категории.

---

## Уровень 3 — Переводы и красная точка

У каждого тега и категории есть переводы — отдельная таблица с парами "язык + текст". Например:

* RU → "Бесплатно"
* EN → "Free"
* DE → "Kostenlos"

Система знает, сколько языков активно (допустим, 3). Если у тега только 2 перевода — в админке рядом с ним появляется  **красная точка** . Это сигнал: "эй, переведи на немецкий".

Это вычисляется каждый раз при запросе, ничего не хранится — просто сравниваются два числа.

---

## Уровень 4 — SEO страницы тегов

Тег "Windows" — это не просто этикетка. У него есть своя страница `/tag/windows` с заголовком, мета-описанием и H1. Это хранится в таблице локализаций прямо рядом с переводом названия.

Пользователь гуглит "лучшие программы для Windows" — попадает на страницу тега, а оттуда уже видит все программы с этим тегом.

---

## Уровень 5 — Связь с программами

Программа может иметь несколько тегов. Это хранится в отдельной bridge-таблице:

```
Photoshop → [Windows, macOS, Платно, Графика]
```

У каждой связи есть два поля: **SortOrder** (в каком порядке показывать теги на карточке) и **IsMain** (один тег — главный, он показывается даже на мини-карточке в списке).

---

## Уровень 6 — Защита от случайных поломок

Если ты пытаешься удалить категорию "Лицензия", а в ней 50 тегов — система не даст это сделать. Вернёт ошибку с человекочитаемым текстом, и кнопка удаления в UI будет серой с подсказкой "сначала перенеси или удали теги".

Это защита от ситуации когда удалил категорию → 50 тегов повисли без привязки → карточки программ сломались.

---

## Как это выглядит для пользователя

1. Администратор создаёт категорию "ОС" с синим цветом
2. Создаёт теги "Windows", "macOS", "Linux" — они все автоматически синие
3. Переводит каждый тег на все языки
4. Привязывает теги к программам
5. Пользователь на сайте видит синие бейджи на карточках программ и может фильтровать

Вот и всё — ничего магического, просто аккуратно выстроенная иерархия с несколькими защитными механизмами.

Что изменено относительно v3.1

| ℹ️ АУДИТ | В версии 3.1 были выявлены следующие проблемы, исправленные в данном документе:1. Противоречие Cascade Delete / Soft Deleteв CategoryTagLocalization — устранено.2. Отсутствие PK в bridge-таблицеProgramTagOfAggregator — добавлен составной PK.3. Ложный B-Tree индекс на Name— заменён на GIN (pg_trgm) с объяснением.4. Риск гонки при SortOrder— добавлено примечание и стратегия.5. Отсутствие UX для Delete Guard— описан ответ API и поведение UI.6. Размытый пункт B1 в чек-листе— разбит на B1a (Fluent API) и B1b (Migration). |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

1. Модели данных (DAL)

Все сущности наследуются от FullAuditableEntityOfAggregator. Это гарантирует наличие полей IsDeleted, DeletedAt, CreatedBy, UpdatedBy — Soft Delete включён по умолчанию.

1.1. CategoryTagOfAggregator (Группы тегов)

Таблица: category_tags_of_aggregator

| Поле  | Тип        | Описание                                                                                              |
| --------- | ------------- | ------------------------------------------------------------------------------------------------------------- |
| Id        | int (PK)      | Auto-increment первичный ключ.                                                                   |
| Slug      | string (100)  | Технический код для URL, напр. license-types. Unique Index.                              |
| IconName  | string? (100) | CSS-имя иконки. Наследуется тегами.                                                 |
| Color     | string? (50)  | CSS-переменная или HEX. Пример: var(--av-tag-purple). Наследуется тегами. |
| SortOrder | int           | Порядок вывода группы (default: 0).                                                        |
| IsActive  | bool          | Видимость группы (default: true).                                                              |

1.2. CategoryTagOfAggregatorLocalization

Таблица: category_tag_localizations

| ⚠️ ВАЖНО | Удаление категории — это Soft Delete (IsDeleted = true). Физического удаления записи не происходит. Поэтому CASCADE DELETE на уровне БД для локализаций не применяется.Локализации скрываются вместе с категорией через условие IsDeleted = false в запросах. FK настраивается как DeleteBehavior.NoAction. |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

| Поле      | Тип        | Описание                                         |
| ------------- | ------------- | -------------------------------------------------------- |
| Id            | int (PK)      | Auto-increment.                                          |
| CategoryTagId | int (FK)      | FK → CategoryTagOfAggregator. DeleteBehavior.NoAction.  |
| LanguageId    | int (FK)      | FK → Language.                                          |
| Name          | string (100)  | Название группы на языке. Required. |
| Description   | string? (500) | Описание группы.                           |

1.3. TagOfAggregator (Теги)

Таблица: tags_of_aggregator

| Поле      | Тип        | Описание                                                                                                 |
| ------------- | ------------- | ---------------------------------------------------------------------------------------------------------------- |
| Id            | int (PK)      | Auto-increment.                                                                                                  |
| Slug          | string (100)  | Уникальный идентификатор. Unique Index.                                                   |
| CategoryTagId | int (FK)      | Обязательная привязка. DeleteBehavior.Restrict — см. п. 3.2.                             |
| Type          | Enum          | Functional(0), Technical(1), OS(2), License(3), Marketing(4), Special(5).                                        |
| Color         | string (50)   | inherit = взять цвет категории. Иначе — своё значение (CSS/HEX).             |
| IconName      | string? (100) | null = взять иконку категории. Иначе — своя.                                       |
| IsFeature     | bool          | Приоритетный тег — вывод в быстрых характеристиках (default: false). |
| SortOrder     | int           | Порядок вывода (default: 0).                                                                        |
| IsActive      | bool          | Видимость тега (default: true).                                                                     |

1.4. TagOfAggregatorLocalization (SEO-модуль)

Таблица: tag_localizations

| Поле        | Тип         | Описание                                              |
| --------------- | -------------- | ------------------------------------------------------------- |
| Id              | int (PK)       | Auto-increment.                                               |
| TagId           | int (FK)       | FK → TagOfAggregator. DeleteBehavior.NoAction (Soft Delete). |
| LanguageId      | int (FK)       | FK → Language.                                               |
| Name            | string (100)   | Required.                                                     |
| Description     | string? (1000) | Полное описание тега.                       |
| MetaTitle       | string? (255)  | SEO — тег `<title>`.                                    |
| MetaDescription | string? (500)  | SEO — мета-описание.                             |
| H1Title         | string? (255)  | SEO — заголовок страницы тега.          |

1.5. ProgramTagOfAggregator (Many-to-Many Bridge)

Таблица: program_tags_of_aggregator

| ⚠️ ВАЖНО | PK — составной: (ProgramId, TagId). Это запрещает дублирующие записи на уровне БД без дополнительного unique constraint. EF Core необходимо явно задать HasKey(x => new { x.ProgramId, x.TagId }) в Fluent API. |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

| Поле  | Тип       | Описание                                                                              |
| --------- | ------------ | --------------------------------------------------------------------------------------------- |
| ProgramId | int (FK, PK) | FK → Program. Часть составного PK.                                            |
| TagId     | int (FK, PK) | FK → TagOfAggregator. Часть составного PK.                                    |
| SortOrder | int          | Порядок тегов внутри карточки программы.                   |
| IsMain    | bool         | Основной тег для мини-карточки программы (default: false). |

2. База данных и Индексация

| ⚠️ ВАЖНО | B-Tree индекс на поле Name НЕ используется при LIKE '%word%' (Contains в EF Core). Для сквозного поиска необходим GIN-индекс через расширение pg_trgm (PostgreSQL). Без него — полный перебор таблицы.Перед созданием GIN-индекса выполнить: CREATE EXTENSION IF NOT EXISTS pg_trgm; |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

| Таблица | Поля                           | Тип           | Обоснование                                                                        |
| -------------- | ---------------------------------- | ---------------- | --------------------------------------------------------------------------------------------- |
| category_tags  | Slug                               | Unique B-Tree    | Маршрутизация по слагу.                                                   |
| tags           | Slug                               | Unique B-Tree    | Прямые ссылки на теги.                                                      |
| tags           | CategoryTagId, IsActive, IsDeleted | Composite B-Tree | Выборка тегов группы для витрины.                                 |
| localizations  | Name                               | GIN (pg_trgm)    | Сквозной поиск через Contains. Требует расширения pg_trgm. |

3. Бизнес-логика (Service Layer)

3.1. Стратегия наследования визуальных свойств

Реализуется в TagMapper (AutoMapper) при формировании DTO. В базе данных хранятся «сырые» значения; вычисленные DisplayColor и DisplayIcon существуют только в DTO.

// Цвет

dest.DisplayColor = (src.Color == "inherit" || string.IsNullOrEmpty(src.Color))

    ? src.Category.Color

    : src.Color;

// Иконка

dest.DisplayIcon = string.IsNullOrEmpty(src.IconName)

    ? src.Category.IconName

    : src.IconName;

3.2. Delete Guard — защита удаления категории

FK между Tag и CategoryTag настроен как DeleteBehavior.Restrict. Прямое удаление категории с активными тегами запрещено на уровне БД. Дополнительно Guard реализуется в сервисе.

// CategoryTagService.DeleteAsync(int id)

var hasTags = await _db.Tags

    .AnyAsync(t => t.CategoryTagId == id && !t.IsDeleted);

if (hasTags)

    throw new BusinessException(

    "CATEGORY_NOT_EMPTY",

    "Нельзя удалить категорию, содержащую теги.");

| 👁️ UX | API возвращает HTTP 409 Conflict с кодом CATEGORY_NOT_EMPTY.Frontend отображает Toast-уведомление: «Сначала переназначьте или удалите теги этой категории».Кнопка удаления в UI категории с тегами — отображается disabled с Tooltip-объяснением. |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

3.3. Сквозной поиск (Cross-Language Search)

Поиск выполняется через IQueryable с использованием Any() по коллекции локализаций. Требует GIN-индекса (см. раздел 2).

query.Where(x =>

    x.Slug.Contains(searchTerm) ||

    x.Localizations.Any(l => l.Name.Contains(searchTerm)));

3.4. Статус RequiresTranslation

Вычисляемое поле в DTO. В UI отображается как красный индикатор (Badge / Dot).

dto.RequiresTranslation =

    entity.Localizations.Count < _languageService.GetActiveLanguagesCount();

3.5. Управление SortOrder

При создании новой записи: SortOrder = (db.Set.Max(x => x.SortOrder) ?? 0) + 1.

| ⚠️ РИСК | Паттерн Max+1 может порождать дубликаты при параллельных запросах (race condition). Для MVP это приемлемо.При росте нагрузки заменить на PostgreSQL SEQUENCE или добавить UNIQUE constraint на (SortOrder, IsDeleted) с обработкой конфликта на уровне сервиса. |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

Пакетная сортировка (Drag-and-Drop): метод UpdateSortOrderAsync(List `<SortItemDto>` items) обновляет массив ID в одной транзакции.

4. Требования к UI (Frontend / Angular)

Список тегов

Цветной Badge с учётом наследования (DisplayColor, DisplayIcon из DTO).

Красный Dot-индикатор для тегов с RequiresTranslation = true.

Кнопка удаления категории с тегами — disabled + Tooltip.

Редактор тега

Выбор категории из выпадающего списка (обязательное поле).

Выбор цвета через пресеты (TagColors constants) или кастомный CSS/HEX ввод.

Рядом с полями Color и IconName — превью «Унаследовано от категории: [значение]».

SEO-вкладка: MetaTitle, MetaDescription, H1Title.

Поиск

Сквозной поиск по всем языкам одновременно.

Debounce 300 мс перед отправкой запроса.

Сортировка

Drag-and-Drop внутри списка.

Вызов UpdateSortOrderAsync при завершении перетаскивания.

5. Fluent API — ключевые настройки

Всё ниже реализуется в AggregatorModelConfiguration.cs (или отдельных файлах конфигурации).

// CategoryTag → Unique Slug

builder.HasIndex(x => x.Slug).IsUnique();

// Tag → Unique Slug

builder.HasIndex(x => x.Slug).IsUnique();

// Tag → Composite index

builder.HasIndex(x => new { x.CategoryTagId, x.IsActive, x.IsDeleted });

// Tag → FK с Restrict

builder.HasOne(t => t.Category)

    .WithMany(c => c.Tags)

    .HasForeignKey(t => t.CategoryTagId)

    .OnDelete(DeleteBehavior.Restrict);

// CategoryLocalization → FK без каскада

builder.HasOne(l => l.CategoryTag)

    .WithMany(c => c.Localizations)

    .HasForeignKey(l => l.CategoryTagId)

    .OnDelete(DeleteBehavior.NoAction);

// ProgramTag → составной PK

builder.HasKey(x => new { x.ProgramId, x.TagId });

// GIN-индекс (raw SQL в миграции)

migrationBuilder.Sql(

    "CREATE INDEX ix_localizations_name_gin"

    + " ON tag_localizations USING GIN (\"Name\" gin_trgm_ops);");

7. Инициализация данных (Seeding)

Для наполнения системы базовыми тегами используется JSON-файл.

### 7.1. Формат JSON (tags-seed.json)

Обеспечивает иерархическую загрузку: сначала Категория, затем её Теги.

```json
{
  "Categories": [
    {
      "Slug": "license-types",
      "IconName": "lucide-shield-check",
      "Color": "var(--av-tag-purple)",
      "SortOrder": 1,
      "Translations": {
        "ru-RU": { "Name": "Типы лицензий", "Description": "..." },
        "en-US": { "Name": "License Types", "Description": "..." }
      },
      "Tags": [
        {
          "Slug": "freeware",
          "Type": "License",
          "Color": null, 
          "IconName": null,
          "SortOrder": 1,
          "IsFeature": true,
          "Translations": {
            "ru-RU": { "Name": "Бесплатно", "MetaTitle": "...", "H1Title": "..." },
            "en-US": { "Name": "Freeware", "MetaTitle": "...", "H1Title": "..." }
          }
        }
      ]
    }
  ]
}
```

### 7.2. Логика работы Seed-сервиса

1. **Ключ идентификации**: Slug.
2. **Алгоритм (Upsert)**: Найти по Slug. Если найдено — обновить (Update), если нет — создать (Insert).
3. **Локализация**: Обновление/создание локализаций для всех указанных BCP-47 кодов.
4. **Валидация**: Отсутствие en-US не блокирует загрузку, но логируется как Warning в консоль.

---

6. Чек-лист реализации

Backend (DAL & API)

B1a. Fluent API конфигурация:

- Unique Index на Slug для CategoryTag и Tag.
- Composite Index на (CategoryTagId, IsActive, IsDeleted) для Tag.
- DeleteBehavior.Restrict для Tag → CategoryTag.
- DeleteBehavior.NoAction для локализаций (Soft Delete совместимость).
- Составной PK (ProgramId, TagId) для ProgramTagOfAggregator.

B1b. Миграция:

- Сгенерировать миграцию AddCategoryTagSystem.
- Добавить в миграцию raw SQL: CREATE EXTENSION IF NOT EXISTS pg_trgm.
- Добавить в миграцию raw SQL: CREATE INDEX ... USING GIN (gin_trgm_ops).

B2. CategoryTagService: реализовать Guard-логику удаления (HTTP 409 + BusinessException).

B3. TagService: реализовать сквозной поиск через IQueryable + Any().

B4. TagMapper (AutoMapper): настроить DisplayColor и DisplayIcon с логикой наследования.

B5. Покрыть Guard-логику юнит-тестом (тест: удаление с тегами → исключение).

B6. Seed Infrastructure: реализовать сервис загрузки tags-seed.json (Upsert logic по Slug).

Frontend (Angular)

F1. Менеджер категорий — CRUD (создание, редактирование, список, удаление с Guard).

F2. Редактор тега — выбор категории, цвет/иконка с превью наследования, SEO-вкладка.

F3. Список тегов — Badge с цветом, Dot для RequiresTranslation.

F4. Сквозной поиск с Debounce 300 мс.

F5. Drag-and-Drop сортировка с вызовом UpdateSortOrderAsync.

Документ финализирован. Все предыдущие версии считаются устаревшими.

Система CategoryTag & Tag — Aggregator v3.5
